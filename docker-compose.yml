version: '3'
services:
  sainterra-mongo:  # Cambié el nombre del servicio a 'sainterra-mongo'
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/sainterra
  mongo:
    image: mongo
    ports:
      - "27017:27017"
